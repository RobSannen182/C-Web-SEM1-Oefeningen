@model IEnumerable<MVCPXLParty2021.Models.GuestParty>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Guest)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Party)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Guest.GuestName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Party.PartyName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GuestPartyId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GuestPartyId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GuestPartyId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
